local uiHandler = require "assets.lua.ui"

local function onClcik(element)
    
end

uiHandler.addUiobject({
    x = 400,
    y = 100,
    w = 100,
    h = 50,
    name = "PlayButton",
    onClick = onClcik
})


